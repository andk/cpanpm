---
comment: "this matches many more distributions than I have tested, but the parameters are pretty harmless in any environment"
match:
  distribution: "^SMUELLER/PAR-"
pl:
  env:
    PERL_AUTOINSTALL: --skip
  expect:
    - "Auto-install"
    - "y\n"
make:
  env:
    PERL_AUTOINSTALL: --skip    
