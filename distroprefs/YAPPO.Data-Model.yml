---
comment: |

  Without Data::MessagePack we only run 20 harmless tests. I suspect
  the fails come more often with MessagePack.

match:
  distribution: '^YAPPO/Data-Model-\d'
depends:
  requires:
    Data::MessagePack: 0
