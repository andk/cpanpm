---
comment: |

  Math::BigInt seems to take infinitely long to compute this test to
  an end. Only if Math::BigInt::GMP is installed, this test is
  computed in a few seconds. Last time verified with Crypt-DH-0.06 and
  Math::BigInt 1.82.

match:
  distribution: "^BTROTT/Crypt-DH-"
disabled: 1
