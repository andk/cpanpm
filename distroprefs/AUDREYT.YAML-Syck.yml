--- 
match:
  distribution: "AUDREYT/YAML-Syck-"
pl:
  env:
    PERL_AUTOINSTALL: --skip
  eexpect:
    mode: anyorder
    timeout: 5
    talk:
      - "Continue installing YAML::Syck"
      - "y\n"
make:
  env:
    PERL_AUTOINSTALL: --skip    
patches:
  - "ANDK/patches/YAML-Syck-1.00-ANDK-01.patch.gz"
