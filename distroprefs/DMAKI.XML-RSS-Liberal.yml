---
match:
  distribution: "DMAKI/XML-RSS-Liberal-"
pl:
  env:
    PERL_AUTOINSTALL: --skip
  eexpect:
    mode: anyorder
    timeout: 5
    talk:
      - "Auto-install"
      - "y\n"
make:
  env:
    PERL_AUTOINSTALL: --skip    
